---
topic: FlowBoard Video Calls UX
updated: 2026-07-09T12:17
---

- (decision) Source: prd.md (final) at _bmad-output/planning-artifacts/prds/prd-FlowBoard-WebRTC-2026-07-09/, confirmed as sole upstream input; addendum.md is technical-how context only.
- (decision) PRD already establishes: 3 named journeys (Maria, Alex, Jordan), Glossary terms, form-factor (desktop web only, Chrome/Edge/Firefox, mobile out of scope), key surfaces (room entry, call view/grid, call controls bar, camera-off/permission-denied placeholder, screen-share-priority layout).
- (decision) Vibe: clean, minimal, modern dark-mode-first, Google Meet aesthetic with developer-tool polish. Professional/sharp for demo.
- (decision) Working mode: FAST PATH, no discovery Q&A batch requested -- draft directly from concrete decisions below, mark gaps [ASSUMPTION], user reviews after.
- (decision) Color system: dark theme, near-black background ~#1a1a1a range; rounded corners on tiles/buttons; accent = calm blue or teal (user left exact hue to me); hang-up button = red/destructive; sans-serif system fonts, good contrast, accessible (WCAG).
- (decision) Screens: (1) Room-entry -- Display Name input, Room ID input, Generate Room ID button, Create/Join button, clean and centered. (2) Call view -- adaptive video grid + bottom control bar.
- (decision) Video grid rules (FR-19/FR-20): 1 participant = full/large tile; 2 = side by side; 3-4 = 2x2; 5+ = grid keeps expanding, tiles shrink. Each tile: video + display-name label + mute indicator + camera-off placeholder (initials/avatar). Subtle connection-issue indicator on affected tiles (FR-10).
- (decision) Screen-share layout (FR-21): large main view of shared screen + participant tiles as thumbnail strip (side or bottom, exact placement left to me).
- (decision) Control bar: mic mute/unmute, camera on/off, share screen (disabled + tooltip when someone else is sharing, FR-12), hang up (red). Always visible, icons + clear active/inactive states.
- (decision) States to design: waiting alone in room; connecting to a peer; active call; screen sharing active; media permission denied (joined with camera/mic off + clear indicator, FR-22).
- (decision) Deliverable: developer-ready UX specification -- component breakdown, layout rules, states, indicators -- saved to UX artifacts location.
- (assumption) No Blazor UI component library specified upstream -- DESIGN.md assumes plain custom CSS, no inherited system.
- (assumption) Accent hue chosen: teal (#2DD4BF), not blue -- differentiates from ubiquitous blue in competing video-call products, still 'calm' per user's brief.
- (assumption) Connection-issue indicator color: amber, distinct from destructive red and primary teal.
- (assumption) Avatar-initials placeholder needs a 6-color deterministic palette (hashed by display name) so multiple off-camera participants stay visually distinguishable -- not specified by user, inferred as necessary.
- (assumption) Room ID rendered in monospace throughout (input + in-call display) -- developer-tool convention for a precise, copy-pasted string; not specified by user.
- (assumption) Thumbnail strip (screen-share layout) placed on the right edge, not bottom -- avoids competing with the floating bottom control bar.
- (assumption) In-call Room ID display + copy-to-clipboard control added -- PRD/user never specified how a participant retrieves the room ID again after joining, needed for UJ-1's teammate-invite flow.
- (assumption) Grid narrow-window behavior: below ~1200px at 5+ participants, grid holds a minimum tile width and scrolls vertically rather than shrinking indefinitely -- resolves PRD FR-19's deferred legibility-bounds open item.
- (event) [NOTE FOR UX] open, not resolved: hover-only tooltips (disabled Share Screen) lack a keyboard-accessible equivalent -- flagged in Accessibility Floor.
- (event) [NOTE FOR UX] open, not resolved: whether clicking a disabled Mic/Camera control (after FR-22 permission denial) re-triggers the browser permission prompt or requires manual browser-settings fix -- flagged in Flow 3.
- (event) Draft of DESIGN.md and EXPERIENCE.md produced (fast path, no discovery Q&A batch). Presented to user for review.
- (decision) User approved all draft [ASSUMPTION] calls as-is (teal, amber, avatar palette, monospace room ID, right-edge thumbnail strip, in-call room ID + copy).
- (decision) NOTE FOR UX 1 resolved: tooltips must trigger on :focus as well as :hover -- keyboard accessibility is now a concrete implementation requirement, not optional.
- (decision) NOTE FOR UX 2 resolved: clicking a disabled Mic/Camera control after permission denial attempts to re-request the browser permission prompt; if permanently blocked, shows inline hint to enable access in browser settings. Deliberately simple -- no blocked-state detection. Marked [ASSUMPTION] to verify during implementation.
- (event) Key-screen mocks rendered: room-entry.html; call-view-grid.html (1/2/4/6 participant breakpoints, muted/connection-issue/camera-off/permission-denied states); call-view-screenshare.html (FR-21 main-view + right thumbnail strip). Promoted from .working/ to mockups/, already linked from EXPERIENCE.md Information Architecture.
- (event) Mock coverage confirmed: both IA surfaces (Room Entry, Call View) fully mocked across all named states -- no spine-only surfaces remain.
- (change) Reviewer gate (rubric walker) ran: 1 critical, 1 high, 5 medium, 6 low. Fixed all 13: removed phantom Hang Up confirmation-dialog claim (contradicted PRD FR-18); added avatar-N-foreground + connection-issue-foreground color tokens; added grid-min-tile-width/narrow-window-breakpoint spacing tokens (replacing raw pixel values in EXPERIENCE.md); added Failure lines to all 3 Key Flows covering PRD-sourced edge cases (Flow 1 mistyped room ID, Flow 2 concurrent share attempt, Flow 3 FR-10 per-pair connection failure); split Component Patterns rows for 1:1 naming with DESIGN.md.Components (Primary/Secondary Button, Room ID Input, Name Label Chip, Mute Indicator, Control Button, Hang Up Button); normalized EXPERIENCE.md frontmatter to name:/portable sources token; added submitting-state and total-local-disconnect (out-of-scope) rows to State Patterns.
- (event) Polish complete: structural review found 0 issues in DESIGN.md, 1 consistency fix in EXPERIENCE.md (stale hover-only tooltip note contradicting resolved Accessibility Floor requirement). Prose review: 1 fix in DESIGN.md (Control Bar description named a component type inconsistently after the component-split fix), 1 fix in EXPERIENCE.md (removed a reference to this document's own editing process from the Hang Up Button row).
- (event) spines finalized
